{
    "port": 8000,
    "apiPath": "/ws",
    "listenTimeout": 40000,
    "clientDeleteTimeout": 60000,
    "logger": {
        "prettyPrint": {
            "messageFormat": "{level}: {msg}",
            "ignore": "time,pid,hostname,reqId",
            "colorize": true
        }
    }
}
